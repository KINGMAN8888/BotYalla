# REVIEW.md — مراجعة شاملة لـ BotYalla V2

> **التاريخ:** 2026-09-10 · **النطاق:** 7,351 سطر Python + واجهة React + قناة واتساب + محرك التسعير
> **الطريقة:** قراءة الكود + تشغيل كل الاختبارات + إثبات عملي (PoC) لكل ثغرة مُدّعاة.
> لا يُدرَج هنا أي بند لم يُتحقَّق منه بتشغيل فعلي.

---

## 0) الخلاصة التنفيذية

البنية الأمنية للمشروع **مدروسة وناضجة**: ويبهوك واتساب مطبَّق بشكل مثالي، ومسارات المال ذرّية ومحصّنة ضد الاحتساب المزدوج، والصلاحيات تُقرأ من القاعدة في كل طلب. الاختبارات في `tests/` (86 اختباراً) تختبر ثوابت حقيقية لا شكليات.

الشرخ الخطير الوحيد كان **ثغرة XSS مخزّنة** مصدرها سطر واحد. أُصلحت هي وكل بند آخر قابل للإصلاح في الكود، وأُغلق كلٌّ منها باختبار انحدار — ارتفع الإجمالي من 91 إلى **123 اختباراً**.

| الخطورة | العدد | الحالة |
|---|---|---|
| 🔴 حرجة | 1 | ✅ **مُصلَحة** |
| 🟠 عالية | 2 | 🟨 الكود أُصلح — **تدوير كلمة المرور و`COOKIE_SECURE` مطلوبان منك** |
| 🟡 متوسطة | 4 | ✅ **كلها مُصلَحة** |
| 🔵 منخفضة | 4 | ✅ **كلها مُصلَحة** |

**كل ما يمكن إصلاحه في الكود أُصلح.** المتبقّي إجراءان تشغيليان لا يمكن أتمتتهما: تدوير `ADMIN_PASS` (§2.1) وضبط `COOKIE_SECURE=1` (§2.2).

---

## 1) 🔴 حرجة — XSS مخزّنة عبر حقن `window.BY` — ✅ مُصلَحة

### الوصف

`react_page()` و`landing()` كانا يحقنان حمولة الصفحة هكذا:

```python
by_json = json.dumps(payload, ensure_ascii=False, default=str)
# القالب:  <script>window.BY = {{ by_json|safe }};</script>
```

`json.dumps` **لا يهرّب** السلسلة `</script>` (ولا فاصلي السطر `U+2028`/`U+2029`). المتصفح يُنهي وسم السكربت عند أول `</script>` داخل أي نص، فينفلت ما بعده كـ HTML حيّ.

### مسارات الاستغلال (كلها أُثبتت عملياً)

| # | المهاجم | الطريق | الضحية والأثر |
|---|---|---|---|
| 1 | أي زائر | يسجّل باسم مستخدم `</script><img src=x onerror=...>` — لا قيد على أحرف الاسم، الطول فقط | **الأدمن** يفتح `/admin/users` → تنفيذ كود في جلسته، وتوكن CSRF معروض في نفس الحمولة (`BY.csrf`) ⇒ اعتماد مدفوعات، ترقية أدوار، **سيطرة كاملة على المنصة** |
| 2 | أي زبون لأي بوت | يكتب النص في محادثة تليجرام/واتساب فيُخزَّن في `lead` | **صاحب البوت** يفتح `/bot/<id>` → تنفيذ كود في جلسته |
| 3 | الأدمن (أو حساب أدمن مخترَق) | `support_email` في إعدادات المنصة | **كل زائر** لصفحة `/landing` العامة |

### الإصلاح المطبَّق

دالة واحدة `_js_json()` في `app.py` تهرّب `<`, `>`, `&`, `U+2028`, `U+2029` قبل الحقن. الهروب يقع **داخل السلاسل فقط**، وجافاسكربت تفكّه تلقائياً — فلا يتغيّر أي سلوك في الواجهة (تحقّقنا: الأيقونات SVG والنصوص العربية سليمة).

```python
def _js_json(payload):
    return (json.dumps(payload, ensure_ascii=False, default=str)
            .replace("<", "\\u003c").replace(">", "\\u003e").replace("&", "\\u0026")
            .replace("\u2028", "\\u2028").replace("\u2029", "\\u2029"))
```

طُبِّقت على **نقطتَي** الحقن: `react_page()` و`landing()`.

### الحارس

`tests/test_xss.py` — 5 اختبارات تغطي المسارات الثلاثة + سلامة الحمولة بعد الهروب.
**تحقُّق مضاد:** الاختبار يفشل على الكود القديم وينجح على المُصلَح، فهو حارس حقيقي لا شكلي.

> **ملاحظة للمستقبل:** أي حقن جديد لبيانات داخل `<script>` يجب أن يمرّ بـ `_js_json` — لا بـ `json.dumps` مباشرة.

---

## 2) 🟠 عالية

### 2.1 كلمة مرور الأدمن الحقيقية داخل مستودع git — 🟨 الكود أُصلح، التدوير مطلوب

`test_full.py` كانت تحتوي كلمة المرور نصاً صريحاً، وهي **مطابقة حرفياً** لـ `ADMIN_PASS` في `.env`. الملف **متتبَّع في git** والريموت `github.com/KINGMAN8888/BotYalla` (يرجّع 404 حالياً ⇒ خاص، لكن القيمة محفوظة في تاريخ المستودع).

**✅ ما أُصلح:** الاختبار صار يقرأ البيانات من البيئة بقيمة افتراضية خاصة به:
```python
ADMIN_USER = os.environ.setdefault("ADMIN_USER", "admin")
ADMIN_PASS = os.environ.setdefault("ADMIN_PASS", "e2e-test-only-pass-123")
```
تُضبط **قبل** استيراد `app` — لأن الوحدة تحمّل `.env` بـ `setdefault`، فالقيمة هنا تسبقه ولا تُقرأ كلمة مرور الإنتاج في الاختبارات إطلاقاً. وأُضيف تأكيد صريح على نجاح دخول الأدمن، فلا يعود الفشل يظهر برسالة مضلِّلة (`'free' != 'pro'`) بدل السبب الحقيقي.

**⬜ ما زال مطلوباً منك:**
1. **غيّر `ADMIN_PASS`** في `.env` وعلى الخادم — القيمة القديمة محفوظة في تاريخ git.
2. لو ستفتح المستودع للعامة يوماً، نظّف التاريخ (`git filter-repo`) أو ابدأ مستودعاً جديداً.

> ✅ `.env` نفسه **غير متتبَّع** — `.gitignore` صحيح.

### 2.2 `COOKIE_SECURE=0` في `.env`

كوكي الجلسة يُرسل بلا علامة `Secure` رغم أن `deploy/nginx.conf` مضبوط على TLS ⇒ الجلسة قابلة للتسرّب عبر أي طلب HTTP عادي.
**المطلوب:** `COOKIE_SECURE=1` في بيئة الإنتاج.

---

## 3) 🟡 متوسطة

### 3.1 لا يوجد Content-Security-Policy — ✅ مُصلَح
كان `deploy/nginx.conf` يحمل `X-Content-Type-Options` و`X-Frame-Options` و`Referrer-Policy` لكن **لا CSP**، و`Strict-Transport-Security` معطّلة بتعليق.

**✅ ما أُصلح:** CSP بـ **nonce يتغيّر مع كل طلب**، تصدر من التطبيق (`_security_headers`) لا من nginx — لأن nginx لا يستطيع توليد nonce لكل طلب، ولأن `add_header` هناك **يضيف** ترويسة ثانية بدل أن يستبدل، فيطبّق المتصفح تقاطع السياستين وتنكسر الصفحة. كل وسم `<script>` في القوالب صار يحمل الـnonce، فـ`script-src` بلا `'unsafe-inline'` إطلاقاً ⇒ **أي سكربت يحقنه مهاجم لن يحمل الـnonce ولن يعمل** — طبقة ثانية مستقلة عن إصلاح البند ١.

```
default-src 'self'; script-src 'self' 'nonce-<متغيّر>'; style-src 'self' 'unsafe-inline' fonts.googleapis.com;
font-src 'self' fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self';
form-action 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'
```
> `style-src` يبقى `'unsafe-inline'` لأن React يكتب أنماطاً في خاصية `style` ولا سبيل لتمرير nonce إليها. و`img-src` يسمح بـ`https:` لأن صور الترحيب والمنتجات روابط يضعها أصحاب البوتات.

**HSTS:** يرسلها التطبيق تلقائياً على الطلبات المؤمّنة فقط (`request.is_secure` عبر `X-Forwarded-Proto`) — إرسالها على http يثبّت الترقية قبل جهوزية الشهادة فيحجب الموقع.

### 3.2 `finalize_payment` ليست معاملة واحدة — ✅ مُصلَح
كانت `decide_payment` → `activate_subscription` → `consume_promo` → `credit_referral` تعمل على **أربعة اتصالات منفصلة**؛ انهيار بينها يترك دفعة «معتمدة» بلا اشتراك مفعَّل.

**✅ ما أُصلح:** مساعد `_conn_or(conn)` في `database.py` يسمح لكل دالة بالعمل على اتصال قائم أو فتح اتصالها الخاص، و`finalize_payment` تفتح اتصالاً واحداً وتمرّره للأربع. عند التمرير لا تُنفَّذ `commit` داخلياً — صاحب الاتصال هو من يبتّ، فإما تثبت الخطوات كلها أو لا شيء منها (الإغلاق بلا commit يتراجع تلقائياً).

**تحقُّق عملي:** حقن انهيار في منتصف التسوية ⇒ الدفعة ترجع `pending` والاشتراك **لا** يُرقّى، ثم تنجح إعادة المحاولة بعد التعافي. كل الحراسات ضد الازدواج باقية كما هي.

### 3.3 تحديد المحاولات على `/login` فقط — ✅ مُصلَح
**✅ ما أُصلح:** `_rate_limited` صارت تأخذ `bucket` يفصل العدّادات، وطُبِّقت على:
- `/register` — 5 محاولات / 10 دقائق لكل IP (منع إغراق الحسابات).
- `/api/promo/check` — 20 / 5 دقائق **لكل مستخدم** لا لكل IP (الحساب هو ما يلزم لبلوغ المسار أصلاً)، وترجّع `429` عند التجاوز.

الفصل بين الدلاء يضمن ألا يقفل استهلاك رصيد التسجيل بابَ الدخول.

### 3.4 `_login_attempts` لا يُنظَّف أبداً — ✅ مُصلَح
**✅ ما أُصلح:** `_prune_attempts` يحذف المدخلات المنتهية (مرة كل دقيقة على الأكثر) ⇒ لا نموّ بلا حدّ. يبقى العدّاد في ذاكرة العملية، فمع أكثر من worker يتضاعف الحدّ الفعلي — و`limit_req` في nginx هو الحاجز الخارجي المكمّل (موثّق في الكود).

---

## 4) 🔵 منخفضة / جودة

### 4.1 تشغيل الاختبارات — ✅ مُصلَح

كانت `tests/test_wa_integration.py` تستدعي `sys.exit()` على مستوى الوحدة، فـpytest يسقط بـ`INTERNALERROR` ولا يشغّل **أي** اختبار في المجلد كله ⇒ CI معطّل تماماً.

**✅ ما أُصلح:** نُقل `sys.exit` داخل `if __name__ == "__main__":` وأُضيفت دالة `test_wa_integration()` ليعمل الملف تحت pytest أيضاً. الملف يبقى قابلاً للتشغيل المفرد كما كان.

**السبب الجذري الأعمق (يبقى بحكم التصميم):** `database.DB_PATH` يُقرأ **مرة واحدة عند الاستيراد** (`database.py:8`)، وكل ملف اختبار يضبط `BOTYALLA_DB` قبل استيراده. تحت pytest تُستورد كل الملفات في **عملية واحدة**، فيفوز أول ملف بالقاعدة ويتشارك الباقون قاعدته فتتصادم بياناتهم (`UNIQUE constraint failed: bots.token`). العزل الذي بُنيت عليه الاختبارات يفترض **عملية لكل ملف**.

**✅ الحل المطبَّق:** `run_tests.sh` — يشغّل كل ملف في عمليته ويلخّص النتيجة، وصالح للاستخدام في CI مباشرة:
```bash
./run_tests.sh
```
> بديل لاحق لو أردت `pytest` على المجلد كله: اجعل `get_conn()` تقرأ `DB_PATH` وقت النداء بدل وقت الاستيراد.

### 4.2 لا تحقق من أحرف اسم المستخدم — ✅ مُصلَح
كان `register` يفحص الطول (≥3) فقط — وهذا ما غذّى المسار الأول في البند ١.

**✅ ما أُصلح:** `USERNAME_RE` (حروف لاتينية/عربية وأرقام و `_ . -`، طول 3–32) مطبَّقة على **المسارات الثلاثة**: التسجيل، وتعديل الاسم من «حسابي»، وإنشاء الأدمن لحساب. تغلق باب إساءة الاستخدام من أصله بدل الاعتماد على طبقة الهروب وحدها.

### 4.3 إيصال الدفع يُكتب على القرص قبل التحقق — ✅ مُصلَح
كان `subscribe_pay` ينفّذ `file.save(fpath)` ثم يتحقق ويحذف عند الفشل — بينما التعليق يقول إن غير الصورة «لا يُحفظ على القرص».

**✅ ما أُصلح:** أُضيفت `pay.validate_bytes(data)` تفحص البايتات مباشرة، وصار `subscribe_pay` يقرأ الرفع إلى الذاكرة (محدود سلفاً بـ`MAX_CONTENT_LENGTH`) ويتحقق **قبل** أي كتابة — فما ليس صورة لا يلمس نظام الملفات إطلاقاً. `validate_image(path)` بقيت وتفوّض إلى `validate_bytes` فلا يفترق حكم المسارين، و`img_hash` صار يُحسب من البايتات مباشرة.

> درس جانبي: الاختبارات التي كانت تضع بديلاً (stub) لـ`validate_image` وحدها لم تعد تكفي — التقطها `test_full.py` فوراً بالفشل، وحُدِّث ليغطّي `validate_bytes` أيضاً.

### 4.4 التوثيق متأخر عن الكود — ✅ مُصلَح

`CLAUDE.md` يأمر كل وكيل ذكي بقراءة `PROJECT_MEMORY.md` **قبل أي تعديل**، وكان الملفان يورّثان تصوّراً خاطئاً عن المشروع:

| الملف | قبل | بعد |
|---|---|---|
| `AGENTS.md` | ✅ كان محدَّثاً | — بلا تغيير |
| `PROJECT_MEMORY.md` | ❌ صفر ذكر لـ React، أكواد الخصم، الأفيليت، `media_store` | ✅ أُعيدت كتابته: القنوات، الميديا، محرّك التسعير، الأفيليت، التذكيرات، القرارات، وسير العمل |
| `README.md` | ❌ صفر ذكر لـ React أو واتساب أو الأفيليت | ✅ حُدِّث: العنوان، مخطط المعمارية، الوحدات، مخطط القاعدة، المميزات، الباقات، متغيّرات البيئة، خطوات البناء والاختبار، فهرس الوثائق |

---

## 5) ✅ ما هو قوي فعلاً (لا تمسّه)

- **ويبهوك واتساب** (`app.py: whatsapp_webhook`) — نموذجي: يرفض قبل الضبط (fail-closed)، HMAC-SHA256 على **البودي الخام**، مقارنة زمن-ثابت لكل من التوقيع وتوكن التحقق، واستثناء CSRF مقصود وموثّق لأن التوقيع هو الحارس.
- **مسارات المال** — التسعير يُحسب في الخادم حصراً ولا يُقرأ أي مبلغ من الفورم · `UPDATE ... AND status='pending'` مستخدم كقفل ذرّي يمنع التفعيل المزدوج بين الويب وزر تليجرام · قيد `UNIQUE(promo_id,payment_id)` يمنع حرق الكود مرتين · حارس `converted_at IS NULL` يمنع ازدواج العمولة.
- **`_revalidate_identity`** — الدور والحظر يُقرآن من القاعدة في **كل طلب**، فالحظر وتغيير الدور يسريان فوراً على الجلسات المفتوحة.
- **`_owned()`** — الملكية مفروضة **داخل الاستعلام** (`get_bot(bot_id, uid())`) لا بفحص لاحق ⇒ لا IDOR. و`bot_media` يضيف `nosniff` ويُنزِّل ما ليس صورة/صوتاً بدل فتحه داخل النطاق.
- **`media_store`** — يشمّ magic bytes ولا يثق بالنوع المُعلَن، ويفرض الحصة والحجم قبل الكتابة.
- **الأساسيات** — كل الـSQL بمعاملات (لا injection) · `werkzeug.security` للتجزئة · `ProxyFix(x_for=1)` مضبوط بشكل صحيح خلف nginx.
- **ثقافة الاختبار** — 86 اختباراً في `tests/` تختبر ثوابت حقيقية (تكرار الرسائل، نافذة الـ24 ساعة، حدود الباقة، رفض التنفيذي المُسمّى صورة).

---

## 6) ما تبقّى — إجراءان تشغيليان فقط

كل بند قابل للإصلاح في الكود أُصلح وأُغلق باختبار. المتبقّي لا يمكن أتمتته لأنه خارج المستودع:

1. **دوّر `ADMIN_PASS`** على الخادم وفي `.env` (§2.1) — القيمة القديمة محفوظة في تاريخ git حتى بعد إصلاح الملف.
2. **اضبط `COOKIE_SECURE=1`** في بيئة الإنتاج (§2.2).

> وإن أردت لاحقاً: `pytest tests/` على المجلد كله يحتاج أن تقرأ `get_conn()` قيمة `DB_PATH` وقت النداء لا وقت الاستيراد (§4.1) — `run_tests.sh` يغني عنه اليوم.

---

## 7) سجل التغييرات في هذه المراجعة

| الملف | التغيير | البند |
|---|---|---|
| `app.py` | `_js_json()` للحقن الآمن في `<script>` | 🔴 1 |
| `app.py` | CSP بـ nonce لكل طلب + HSTS على HTTPS فقط (`_csp_nonce` / `_security_headers`) | 3.1 |
| `app.py` | `_rate_limited` بدلاء منفصلة + `_prune_attempts` + تطبيقها على التسجيل وفحص الأكواد | 3.3 · 3.4 |
| `app.py` | `USERNAME_RE` على التسجيل و«حسابي» وإنشاء الأدمن لحساب | 4.2 |
| `app.py` | `subscribe_pay` يتحقق من البايتات قبل أي كتابة على القرص | 4.3 |
| `database.py` | `_conn_or()` + معاملة واحدة لتسوية الدفعة | 3.2 |
| `payments.py` | `validate_bytes()` و`validate_image()` تفوّض إليها | 4.3 |
| `templates_web/*.html` | `nonce` على كل وسم `<script>`/`<style>` | 3.1 |
| `deploy/nginx.conf` | توثيق أن CSP تصدر من التطبيق + مكان HSTS الصحيح | 3.1 |
| `test_full.py` | كلمة المرور من البيئة + تأكيد دخول الأدمن + تغطية `validate_bytes` | 2.1 · 4.3 |
| `tests/test_wa_integration.py` | `sys.exit` تحت `__main__` + دالة اختبار لـpytest | 4.1 |
| `tests/test_xss.py` | **جديد** — 5 اختبارات تقفل ثغرة الحقن | 🔴 1 |
| `tests/test_hardening.py` | **جديد** — 18 اختباراً تقفل بنود §3 و§4 | 3.x · 4.x |
| `run_tests.sh` | **جديد** — عملية مستقلة لكل ملف (صالح لـCI) | 4.1 |
| `PROJECT_MEMORY.md` · `README.md` | تحديث ليطابقا الواقع الحالي | 4.4 |
| `REVIEW.md` | **جديد** — هذا التقرير | — |

**نتيجة الاختبارات بعد كل التغييرات:** 10 ملفات · **123 اختباراً** · كلها ناجحة · لا انحدار.

```
  test_flow.py                  PASS (5)
  test_hardening.py             PASS (18)  ← جديد
  test_media.py                 PASS (21)
  test_wa_integration.py        PASS (9)
  test_wa_media.py              PASS (10)
  test_wa_templates.py          PASS (24)
  test_webhook.py               PASS (9)
  test_whatsapp.py              PASS (17)
  test_xss.py                   PASS (5)   ← جديد
  test_full.py                  PASS (5)   ← يعمل الآن بلا أي متغيّر بيئة
```

> كل اختبار جديد جرى التحقق منه بأنه **يفشل على الكود قبل الإصلاح** — حارس حقيقي لا شكلي.

---

## 8) المراجعة الثانية — تدقيق الإصلاحات نفسها (2026-09-11)

> أُعيد فحص كل تغيير في هذا التقرير بتشغيل فعلي في **نسخة معزولة** من المستودع (بلا `.env` ولا قاعدة حقيقية).
> جوهر الإصلاحات سليم: التسوية الذرّية لا تفتح اتصالاً متداخلاً داخل المعاملة (فلا قفل ذاتي) · CSP لا تكسر الواجهة (لا `blob:` ولا iframe ولا `fetch` خارجي ولا `eval` في الحزمة) · HSTS تعمل فعلاً لأن `ProxyFix` مضبوط بـ `x_proto=1`.
> لكن ظهرت 6 مشكلات — **واحدة حرجة أدخلها سكربت الاختبارات نفسه.**

| # | الخطورة | المشكلة | الحالة |
|---|---|---|---|
| 8.1 | 🔴 حرجة | `run_tests.sh` كان ينفّذ `rm -f botyalla.db` في جذر المستودع — وهو `WorkingDirectory` على الخادم (`/opt/botyalla`، و`backup.sh` يؤكد أن القاعدة هناك). تشغيل الاختبارات على الخادم — كما تطلب قائمة ما قبل الإطلاق — كان **سيحذف قاعدة الإنتاج**. ولا يوجد اختبار ينشئ `botyalla.db` أصلاً (تحقّقنا بتشغيل الملفات كلها). | ✅ أُزيل مع تحذير صريح في السكربت و`AGENTS.md` |
| 8.2 | 🟡 متوسطة | `_prune_attempts` كان ينظّف **كل** الدلاء بنافذة الدلو المُستدعي: نداء دخول (5 دقائق) يمسح عدّاد التسجيل (10 دقائق) في منتصفه ⇒ حدّ التسجيل فعلياً نصف قيمته. والمرور المباشر على القاموس مع 4 خيوط قد يرمي `RuntimeError` فيسقط الدخول بـ500. | ✅ نافذة لكل دلو + لقطة `list(...)` |
| 8.3 | 🟡 متوسطة | «حسابي» يرسل الاسم الحالي دائماً (`defaultValue`)، ففحص `USERNAME_RE` عليه كان يمنع أي حساب قديم لا يطابق القاعدة (بريد، مسافة) من **تغيير كلمة مروره**. | ✅ الفحص عند تغيير الاسم فقط |
| 8.4 | 🔵 منخفضة | `USERNAME_RE` كان يقبل الكتلة العربية كاملة (U+0600–06FF) وفيها محارف غير مرئية (U+061C علامة الاتجاه، U+0600–0605، U+06DD) والتشكيل ⇒ اسم يطابق «admin» بصرياً في قائمة الأدمن. و`$` كانت تقبل سطراً جديداً في النهاية. | ✅ الحروف الأساسية والأرقام والموسّعة فقط + `\Z` |
| 8.5 | 🔵 منخفضة | فحص كود الخصم يرجّع 429 بعد الحدّ، لكن الواجهة كانت تتجاهل أي ردّ `ok:false` ⇒ زرّ لا يفعل شيئاً بلا تفسير. | ✅ في المصدر — **يحتاج `npm run build`** |
| 8.6 | 🔵 منخفضة | صفحات الدخول والتسجيل (بلا قشرة) لا تعرض رسائل flash إطلاقاً ⇒ «بيانات دخول غير صحيحة» و«محاولات كثيرة» لا تظهر للمستخدم. | ✅ مبنيّ ومُتحقَّق منه في المتصفح |
| 8.7 | 🟠 عالية | **أول دخول/تسجيل في جلسة فارغة يُرفض بـ 400 «CSRF token invalid».** `react_page` كانت تقرأ `session["_csrf"]` لحمولة `BY.csrf` **قبل** أن ينشئه معالج السياق، فتخرج الصفحة بتوكن فارغ. يصيب: زائراً يفتح `/login` أو `/register` مباشرة (رابط إعلان، إحالة)، وكل مستخدم بعد «خروج»، وبعد استرجاع كلمة المرور. اكتُشف في المتصفح لا في الاختبارات — كل الاختبارات كانت تزرع `_csrf` مسبقاً. | ✅ `_csrf_token()` ينشئه عند أول حاجة + 4 اختبارات تبدأ بجلسة فارغة (تفشل على الكود القديم) |

**ما زال مفتوحاً (إجراءات منك):** `.env` المحلي ما زال يحمل **نفس** كلمة المرور المسرّبة في تاريخ git (تحقّقنا بالمقارنة دون طباعتها) ⇒ L-01 لم يُنفَّذ بعد. و`COOKIE_SECURE=0` محلياً طبيعي، لكن تأكد أنها `1` على الخادم.

**ملاحظة للمستقبل:** حدّ التسجيل (5 لكل IP في 10 دقائق) قد يضيق على شبكات الموبايل المصرية (CGNAT — مئات المستخدمين خلف IP واحد). راقبه بعد الإطلاق.

**نتيجة الاختبارات بعد المراجعة الثانية وبنود L-03/04/05/06/09:** 13 ملفاً · **164 فحصاً** · كلها ناجحة (آخرها: صفحة 400 لم تعد تحمل `'nonce-'` فارغة في CSP). والواجهة مبنيّة (`npm run build`) ومُتحقَّق منها في المتصفح: رسالة خطأ الدخول · حقل الإيميل · صفحتا الاسترجاع · رفض الرابط المستعمَل · أول دخول بعد الاسترجاع.
