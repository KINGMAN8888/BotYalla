# PROJECT_MEMORY.md — ذاكرة مشروع BotYalla

> ملف السياق الكامل للمشروع: ما هو، ما تم إنجازه، القرارات المهمة، وما تبقّى. اقرأه أنت والوكيل الذكي قبل أي عمل، مع `AGENTS.md`.
>
> **آخر تحديث:** 2026-09-13 (بطل «ضغطة واحدة» وتحديث الرئيسية للمزايا الجديدة + [docs/ONE_TAP_SETUP.md](docs/ONE_TAP_SETUP.md)). قبله 2026-09-12: خطة ملاحظات المختبِر كاملة — [docs/TESTER_FEEDBACK_PLAN.md](docs/TESTER_FEEDBACK_PLAN.md) §10.

---

## 1) ما هو المشروع
**BotYalla** — منصة SaaS عربية/إنجليزية تتيح لأصحاب الأنشطة إنشاء وتشغيل **بوتات محادثة بدون برمجة** على **تليجرام وواتساب**، مع اشتراكات ودفع محلي. المالك: **Youssef Alsherief** (info@botyalla.com · 201097585951 · youssefalsherief.tech).

- **الخادم:** Python + Flask + SQLite + python-telegram-bot + httpx (لواتساب Cloud API).
- **الواجهة:** React 19 + Vite 8 + Tailwind 4 — تُبنى إلى `static/dist/` وتُقدَّم من Flask. عربي/إنجليزي (RTL/LTR)، تصميم داكن.
- **النموذج الربحي:** باقات شهرية (مجانية / احترافية 199ج / الأعمال 499ج) + دفع محلي (فودافون كاش/انستاباي/بنكي) + بوتات مخصّصة عند الطلب. مع **أكواد خصم** و**نظام أفيليت**.

---

## 2) الحالة: ✅ منجز ومختبَر

### الأساس
- **الحسابات والأدوار (RBAC):** admin / support / user. أول مستخدم = admin. حساب افتراضي عند أول تشغيل يُضبط عبر `ADMIN_USER`/`ADMIN_PASS` في `.env`. `_revalidate_identity` يعيد قراءة الدور والحظر **من القاعدة في كل طلب**، فالتغيير يسري فوراً على الجلسات المفتوحة.
- **قوالب البوتات (7):** `flow` باني محادثات · `store` متجر · `booking` حجوزات · `customer_service` خدمة عملاء · `faq` أسئلة شائعة (محرّك قائمة) · `feedback` تقييم · `support` تذاكر — الثلاثة الأخيرة بقوالب جاهزة (`PRESET_FLOWS` / `DEFAULT_MENU_ITEMS`).
- **باني الفلو (No-Code)** لأنواع `flow/customer_service/feedback/support`، ومحرّر قائمة (سؤال←إجابة) لـ `faq`.
- **حالة المحادثة في القاعدة** (جدول `chat_state`) لا في الذاكرة — المحادثات الجارية تنجو من إعادة تشغيل الخدمة.
- **الإيميل واسترجاع الحساب:** عمود `users.email` اختياري وفريد (فهرس جزئي). `/forgot` ← رابط لمرة واحدة صالح ساعة (`password_resets` تخزّن **تجزئة** التوكن) ← `/reset/<token>`. الرابط من `PUBLIC_URL` لا من ترويسة Host، والردّ واحد سواء وُجد الحساب أم لا. تغيير كلمة المرور يُنهي كل الجلسات الأخرى (بصمة `pwv` في الجلسة).
- **`mailer.py`:** SMTP best-effort — لا يرمي أبداً، خيط خلفي، 5 رسائل/عنوان/ساعة. بلا `SMTP_HOST`/`SMTP_FROM` لا يُرسل شيء. يرسل روابط الاسترجاع و**إيصالات الدفع** (بعد `finalize_payment`، خارج المعاملة).
- **السجل:** `setup_logging()` من `bootstrap()` ← stderr + `logs/botyalla.log` (10MB×5). كل 500 يُسجَّل بالـtraceback ويُشعَر به الأدمن على تليجرام (مرة لكل خطأ كل 10 دقائق). الأحداث المالية تُسجَّل بعد commit. `httpx` على WARNING (روابطه تحمل توكن البوت).

### القنوات (طبقة `channels/`)
- **مجرّدة عبر `channels/base.py`** — كل قناة تنفّذ `send_text` / `send_buttons` / `send_image` / `normalize` / `fetch_media`.
- **تليجرام** (`channels/telegram.py`) — polling.
- **واتساب** (`channels/whatsapp.py`) — WhatsApp Cloud API عبر **webhook** على `/wh/whatsapp`، مع احترام نافذة الـ24 ساعة، ومنع تكرار الرسائل، وحدّ شهري للرسائل الصادرة حسب الباقة.
- **قوالب واتساب** (`channels/wa_templates.py`) — إنشاء/حذف/سرد قوالب Meta مع تحقق كامل من القواعد (الاسم، الفئة، حدود الطول، ترقيم المتغيّرات).
- **الميديا** (`media_store.py`) — ملفات العملاء الواردة تُحفظ خارج `static/`، والنوع يُستنتج من **magic bytes** لا من الترويسة المُعلَنة، بحصّة شهرية وحدّ حجم لكل نوع. تُقدَّم فقط لمالك البوت عبر `/bot/<id>/media/<mid>`.

### المال
- **الدفع والتحقق المزدوج:** رفع اسكرين → فحص البايتات **قبل** أي كتابة على القرص → فحص آلي (صورة صحيحة + بصمة تكرار SHA-256 + OCR اختياري) → **تنبيه تليجرام بزرّي موافقة/رفض** + صفحة أدمن. **لا تفعيل تلقائي.**
- **محرّك التسعير** (`_plan_pricing` / `validate_promo` / `quote` في `app.py`) — **المصدر الوحيد للمبالغ**. المبلغ يُحسب في الخادم ولا يُقرأ من الفورم أبداً.
- **أكواد الخصم (`promos`):** نسبة أو مبلغ ثابت · صلاحية · حد استخدام · مقيّدة بباقة · مرة واحدة لكل مستخدم. لا تُستهلك إلا **عند اعتماد الدفعة** (قيد `UNIQUE(promo_id,payment_id)` يمنع الاحتساب مرتين).
- **الأفيليت (`affiliates`/`referrals`):** كود إحالة لكل مستخدم، `?ref=CODE` يُلتقط في الجلسة ويُربط عند التسجيل، والعمولة تُحتسب عند **أول دفعة معتمَدة** (حارس `converted_at IS NULL` يمنع الازدواج).
- **تسوية ذرّية:** البتّ والتفعيل وحرق الكود واحتساب العمولة في **معاملة واحدة** (`_conn_or` في `database.py`) — إما تثبت كلها أو لا شيء منها.
- **تجاوزات التسعير:** الأدمن يضبط سعراً أو نسبة خصم لكل باقة من `/admin/pricing`.
- **الاشتراكات:** 30 يوماً من تاريخ الاشتراك. التجديد المبكر على **نفس** الباقة يُضاف إلى المتبقّي؛ تغيير الباقة أو اشتراك منتهٍ يبدأ من الآن. الأدمن/الدعم «غير محدود».
- **تذكيرات الاشتراك:** خيط خلفي في `bot_manager` (`_reminder_loop`) ينبّه قبل الانتهاء بـ3 أيام عبر بوت المنصة، و`reminder_log` يمنع التكرار ويُمسح عند التجديد.

### الواجهة والإدارة
- **طبقة React:** Flask يبقى مسؤولاً عن التوجيه والصلاحيات والنماذج، وReact يرسم فقط. النماذج تظل POST عادية بـ CSRF — **لا SPA ولا API جديدة**. الحمولة تُحقن في `window.BY` عبر `_js_json()` (لا `json.dumps` مباشرة — راجع REVIEW.md §1).
- **صفحات:** `landing` عامة · لوحة المستخدم · `bot_detail` · الباقات/الدفع/الفواتير · «حسابي» · `terms`/`privacy` · `/healthz`.
- **لوحة أدمن كاملة:** نظرة عامة (KPIs + رسوم) · مستخدمون (أدوار/حظر/باقة) · مدفوعات (موافقة/رفض + عرض الإيصال والفحص المفصّل) · طلبات مخصّصة · **التسعير** · **أكواد الخصم** · **الأفيليت**.
- **بوت المنصة (للأدمن):** `/stats` `/pending` (بأزرار) `/users` `/revenue` + إشعار بكل حركة.
- **وكيل الـ AI:** يضبط البوت من وصف النشاط (Gemini/Groq) + مولّد احتياطي بلا مفتاح. **المفتاح للأدمن فقط**، والميزة مقيّدة بالباقة.

### ملاحظات المختبِر (2026-09-12) — التفاصيل في docs/TESTER_FEEDBACK_PLAN.md §10
- **إنشاء بوت بضغطة** (Telegram Managed Bots، Bot API 9.6): `managed_bots.py` + بوت المنصة. يحتاج تفعيل «Bot Management Mode» لبوت المنصة من BotFather Mini App — حالته في «إعدادات المنصة». زر الإنشاء = رابط `t.me/newbot/<platform>/<username>?name=` (زر inline). **لا تستخدم زر لوحة المفاتيح `request_managed_bot`**: تليجرام iOS فتحه كشاشة «Forward» فارغة (2026-09-13).
- **الوصول للبوت:** رابط/QR (SVG عبر `segno`)/ملصق A4، ومصادر الدخول `src_*` في التحليلات.
- **وكيل الإعداد:** أسئلة ← تصميم ← معاينة ← تطبيق ← نسخ واسترجاع (`bot_config_versions`). مصمّم احتياطي ذكي بلا مفتاح.
- **أوضاع الرد:** فلو · هجين · عقل البوت (مدفوع: حصة `plans.FEATURES.ai_replies` ثم المحفظة بـ`ai_reply_price` بالقروش).
- **صندوق الوارد:** `messages` + `conversations`، تولٍّ يدوي يوقف البوت للعميل، عودة تلقائية بعد 12 ساعة.
- **مكتبة الوسائط:** `assets` + `asset_refs` (`uploads/assets/`)، خطوة «عرض صورة/فيديو» = فيديو تفاعلي.
- **تشفير التوكنات:** Fernet + فهرس أعمى `token_idx`؛ المفتاح `FERNET_KEY` أو `.token.key` بجانب القاعدة (يُنسخ احتياطياً).
- **دليل تفعيل الإنشاء بضغطة:** [docs/ONE_TAP_SETUP.md](docs/ONE_TAP_SETUP.md) — زر **Open** في BotFather ← بوت المنصة ← Bot Management Mode ← «حفظ» في إعدادات المنصة. (`/newapp` = Mini App، لا علاقة له.)

### الدفع والدعم (2026-09-13)
- **سبب «OCR غير متاح» على السيرفر:** `botyalla.service` كان يحصر `PATH` في venv فلا يُرى `/usr/bin/tesseract`، و`--update` لم يكن يثبّته. أُصلح الاثنان (+ `pytesseract`/`Pillow` في requirements، و`payments._probe` يبحث عن tesseract بنفسه). سكربت النشر يطبع حالة OCR في التحقق.
- **محرك الإيصالات 2** (`payments.py`): أرقام عربية · لقطات ليلية · أرقام مخفية جزئياً (`010****951`) · مطابقة حساب المنصة (فودافون/انستاباي/IBAN/اسم صاحب الحساب). يرفض قبل الحفظ: ليس إيصالاً · مستلم آخر · مبلغ آخر · إيصال مستخدم. الأدمن يرى حالة المحرك وعدد المرفوض في «إدارة المدفوعات».
- **الدعم والشكاوى** (`/support` · `/admin/tickets` · `support_desk.py`): تنبيه فوري لكل أدمن على بوت المنصة بزرّ «تم الحل»، والرد بـ Reply من تليجرام يصل العميل (صفحة الدعم + تليجرام لو ربط + إيميل).
- **«اختبر التنبيهات»** في إعدادات المنصة: يرسل رسالة تجربة ويعرض سبب الفشل (أشهره: الأدمن لم يضغط Start في بوت المنصة).
- **ثغرة أُغلقت:** صفحات الاشتراك/الشحن/طلب بوت مخصّص كانت تمرّر `db.all_platform()` كاملة للمتصفح (توكن بوت المنصة · سرّ واتساب · مفتاح AI) ← `_public_plat()`. **دوّر التوكنات الثلاثة** لأنها كانت مكشوفة لأي مستخدم مسجّل.
- **Direct Send (واتساب، بيتا)** في صفحة البث: نص بلا قالب لكل المشتركين. كان مجانياً والفئة من الفورم (utility/authentication) والمنصة تدفع لـMeta ← صار يُخصم من المحفظة بسعر الرسالة ويُردّ ما لم يصل، والفئة ثابتة utility (AGENTS §20). يحتاج أن يكون حساب واتساب مشتركاً في بيتا Meta وإلا يفشل الإرسال ويُردّ الرصيد كله — والعميل يُبلَّغ بذلك كاملاً: صندوق تنبيه في الصفحة قبل الإرسال (`bc_direct_beta_*`) وتأكيد يذكره، ولو رُفض الكل رسالة تشرح السبب الأرجح والردّ الكامل وتوجّهه لـ«قالب معتمد».
- **`Select` في `kit.jsx`** (قائمة بمظهر المنصة فوق `<select>` مخفي): `onChange` يستلم العنصر الحقيقي بعد كتابة القيمة فيه — لازم لـ`e.target.form.submit()` في تغيير الدور/الباقة بلوحة الأدمن (كان يرمي TypeError ولا يحفظ). يعمل بالكيبورد.
- **ربط واتساب بمساعدة الفريق** (مشمول في باقات واتساب، سطر في مزايا باقة «واتساب»): العميل يضغط «سيبها علينا» في خطوة واتساب ← `POST /whatsapp/assist` ← تذكرة `wa_setup` (طلب مفتوح واحد) تصل الفريق فوراً. الفريق يربط من «تذاكر الدعم» ← «ربط واتساب للعميل» (`/admin/tickets/<id>/wa-connect`): البوت في حساب العميل وبحدود باقته، ورسالة له في التذكرة. لا نطلب كلمة سر فيسبوك أبداً — العميل يضيف الفريق مسؤولاً في Meta Business.
- **قفل واتساب:** الواجهة كانت تفتحه لو غاب `waAllowed` من الحمولة (`!== false`) ← صار `=== true` مع قفل «مدفوعة» وزر «رقّي باقتك». الخادم كان يرفض أصلاً؛ الأدمن والدعم مستثنون لحساباتهم (لذا يظهر لهم مفتوحاً).

### تقرير ما قبل الإطلاق (2026-09-14) — `business/LAUNCH_AUDIT_EN.md`
- **أُصلح في الكود (باختبارات `tests/test_launch_audit.py`):** C-1 الحملة عند المهلة كانت تُردّ كاملة وهي تصل ← عدّاد حيّ + توقف نظيف · C-6 البث داخل الطلب يتجاوز nginx فيُخصم مرتين ← خلفية + حملة واحدة لكل بوت + بطاقة «آخر حملة» · C-5 رد AI لم يصل كان يُخصم ← `ai_reply_release` · C-2 OCR بلا حد ← حدّ + قراءتان متزامنتان + مهلة 10ث · C-3 مسارات شبكة بلا حد ← حدود · C-4 `/ai-setup` بلا حصة ← حُذف · I-1 فهارس · I-2 تنظيف events · I-3/I-4 CSV آمن وكامل · I-5 `working_days` · I-6 `/logout` POST · `/pricing` مفهرسة · www→المضيف القانوني · تخزين dist سنة (hash) · لا `admin1234` افتراضية · الوثائق (USER_GUIDE · ARCHITECTURE · README · HOSTINGER).
- **ما زال عليك (خارج الكود):** تفعيل Bot Management Mode وتجربته على موبايل وكمبيوتر · أداة تحليلات (التقرير يقترح Umami ذاتي الاستضافة) · SPF/DKIM/DMARC · تأكيد `PUBLIC_URL` على الخادم · تغيير `ADMIN_PASS` · UptimeRobot على `/healthz` · تجربة استرجاع نسخة احتياطية · السجل التجاري/Meta Tech Provider/ترخيص حماية البيانات (قانون 151/2020، موعد 1 نوفمبر 2026).
- **الفجوة المعروفة أُغلقت (2026-09-14):** ردود قوالب المتجر/الحجز/القائمة والترحيب صارت تظهر في صندوق الوارد. واكتُشف معها عطل أقدم: إشعار المالك بطلب المتجر/الحجز كان يرمي TypeError فلا يصل ويعلق العميل ← `_tell_owner`.

### نمو وتشغيل (2026-09-14)
- **إحصائيات الزوار** (`/admin/analytics`، للمالك): زوار الصفحات العامة ومصادرهم (`?utm_source=`) والأجهزة، وقمع التسجيل على دفعة المسجّلين (سجّل ← أنشأ بوتاً ← شغّله ← أول رسالة عميل ← دفع). بديل Umami داخل المنصة: بلا كوكي تتبّع ولا سكربت خارجي.
- **صور المعاينة** `og-ar.png`/`og-en.png`: 337KB ← ~100KB (median-cut 256 لوناً، بلا تشوّه ظاهر).
- **المراقبة** `docs/MONITORING.md` + `tools/uptimerobot_setup.py` (يشغّله المالك بمفتاحه). **البريد** `docs/EMAIL_DNS.md` — لا SPF/DKIM/DMARC ولا MX على الدومين حالياً.
- **إضافة «تحصيل المدفوعات»** (لكل بوت متجر على تليجرام، السعر `addon_pay_price` من إعدادات المنصة، افتراضي 99ج/شهر): العميل يحوّل على حسابات صاحب البوت ويرسل الإيصال للبوت ← فحص آلي ← صاحب البوت يؤكد/يرفض (تليجرام أو اللوحة) ← العميل يُبلَّغ. سطر باقة التاجر «تحصيل فودافون كاش وانستاباي» كان وعداً بلا ميزة ← صار «إضافة لكل بوت». **ليست بعد:** بوتات الحجز والفلو وواتساب.
- **البريد بهوية العلامة + «رسائل البريد» (2026-09-14):** قالب موحّد لكل الرسائل (شعار مضمَّن، رأس داكن بتدرّج العلامة، شارة نوع الرسالة، تذييل بروابط الموقع). صفحة المالك `/admin/emails`: أخبار/عروض للموافقين + إشعارات خدمة، شرائح (مدفوع · مجاني · بلا بوت · ينتهي خلال 7 أيام · منتهٍ · باقة)، قوالب بداية، معاينة حيّة كما تصل (كمبيوتر/موبايل)، تجربة على إيميل الأدمن، سقف يومي (افتراضي 250)، إيقاف/استئناف بلا تكرار، روابط الموقع بـ`utm_source=email`. موافقة من التسجيل و«حسابي»، وإلغاء بضغطة. تذكير انتهاء الاشتراك صار يصل بالإيميل (سياسة الخصوصية كانت تعد به ولم يكن موجوداً). `Reply-To` للدعم. **إيميل الدعم الرسمي `info@botyalla.com`** بدل الإيميل الشخصي في كل مكان.
- **نظام الحساب (2026-09-15):** تسجيل كامل (نوع الحساب فرد/شركة/مؤسسة · اسم مستخدم بفحص حيّ وتفرّد واقتراحات · بريد **إجباري** · موبايل بمفتاح الدولة · السن 18+ · كلمة مرور قوية بعدّاد ومتطلبات + تأكيد · الموافقة على الشروط). **تأكيد البريد إجباري قبل أي استخدام للحسابات الجديدة** (قرار المالك) بكود 6 أرقام أو رابط بضغطة؛ القديمة لا تُحجب. تأكيد الهاتف **مجاناً عبر تليجرام** (قرار المالك — لا SMS). الدخول باسم المستخدم أو البريد، و**جوجل/فيسبوك** (يظهران بعد ضبط المفاتيح — docs/SOCIAL_LOGIN.md). «حسابي»: بيانات النشاط · التحقق · طرق الدخول. قائمة المستخدمين للأدمن تعرض البريد/الهاتف وتأكيدهما ونوع الحساب.

### «عقل البوت» مساعد مبيعات ودعم + بوت المنصة الرسمي (2026-09-19)
- **تعليمات النموذج الجديدة** (`ai_agent.BRAIN_SYSTEM`): يفهم الاحتياج ثم يرشّح ويختم بخطوة تالية واضحة، بلا ضغط ولا استعجال مزيّف، ويرحّب في أول رسالة فقط. **ضوابط Meta**: في حدود النشاط فقط (ليس مساعداً عاماً — ممنوع على واتساب للأعمال)، لا يطلب كلمات سر/أكواد تحقق/بطاقات/رقم قومي، لا نصائح طبية/قانونية/مالية، يعترف أنه مساعد آلي، يحوّل لموظف عند الشكوى، ويحترم طلب الإيقاف.
- **أزرار الرد السريع** (`suggestions`، ≤3 بعنوان ≤20 حرفاً) تُرسل أزرار واتساب/لوحة تليجرام — لا داخل خطوة فلو.
- **رسالة البداية وأزرارها** (`ai_welcome` · `ai_starters`) من كارت «طريقة الرد» — تحت صورة/فيديو الترحيب مباشرة.
- **مساعد BotYalla الرسمي** (`platform_kb.py`، مفتاح أدمن على بوت حساب الإدارة): أسعار الباقات بعد تجاوزات المالك، المزايا، الدفع، الروابط من `PUBLIC_URL`، وترحيب + أزرار جاهزة.
- **STOP / «إيقاف الرسائل»** ← `bot_users.opted_out` ← خارج كل بثّ وحملة؛ «تفعيل العروض» يعيده.
- **⚠️ سبب «البوت غبي» (2026-09-19):** جوجل سحبت `gemini-2.5-flash` و`-lite` («no longer available to new users» 404)، فكل رد ذكي كان يفشل بصمت ويسقط للفلو («اسمك؟ رقمك؟»). الآن: `gemini-3.6-flash` ← `gemini-3.5-flash` ← `gemini-flash-latest` تلقائياً عند 404/429/5xx، والمفتاح في ترويسة لا في الرابط. **«اختبر المفتاح المحفوظ»** و«آخر خطأ» في `/settings` (`ai_last_error` · `ai_last_ok_at`). العميل لا يعلق في الفلو الاحتياطي: كل رسالة تجرّب الذكاء أولاً. بوت المنصة الرسمي بلا ذكاء يرد من `platform_kb.offline_reply` لا من الفلو.

- **السرعة:** ردود العملاء على `gemini-3.5-flash-lite` (≈1.3ث بدل 6-9ث) ثم الأقوى احتياطاً (`GEMINI_FAST_MODELS`)؛ وكيل الإعداد على الأقوى. واتساب: علامتا القراءة و«يكتب…» فور وصول الرسالة (`WhatsAppChannel.mark_read`، لا يمرّ بالعدّاد). ⚠️ مفتاح Gemini على الطبقة المجانية يعطي 429 بعد طلبات قليلة في الدقيقة — فعّل الفوترة في Google AI Studio للإنتاج.
- **التحويل لموظف لا يُسكت البوت** (كان 12 ساعة صمت): تنبيه على تليجرام (`inbox_relay.py`، وسم `#C<bot>:<peer>`، مرة كل 20 دقيقة للمحادثة) + البوت يكمل. **Reply على التنبيه من تليجرام يصل العميل** (صاحب البوت/أدمن المنصة لبوت الإدارة فقط · نافذة واتساب 24س · يسجَّل «human» ويتولّى) وزرّ «رجّع المحادثة للبوت».
- **الشكر والتقييم** بلا نموذج: «شكراً» ← «العفو» + 3 أزرار (ممتاز · كويس · محتاج تحسين) مرة/24س؛ حدث `rating` (3/2/1) يظهر في كارت «طريقة الرد»، والسلبي يطلب ملاحظة ← عميل محتمل + تنبيه. مفتاح `ai_rating`.
- **بوت المنصة على تليجرام = المساعد الرسمي:** أي زائر يكتب لبوت المنصة (غير أوامر الأدمن وردود التنبيهات) يمرّ بـ`flow_engine.handle_message` على صفّ `platform_kb.official_bot()` بـ peer `tg:`. الرد اليدوي لهم من بوت المنصة (`_send_to_peer`)، وحملات واتساب تستبعد `tg:` (`_SAME_CHANNEL` في `list_bot_peers`)، و`TelegramChannel.cache_refs=False` لقناة المنصة (مرجع الوسائط مشترك بالمفتاح مع واتساب).

- **مزوّدون مجانيون في سلسلة واحدة** (`ai_agent.PROVIDERS` · `key_chain` · `call_chain`): Groq · Cerebras · Gemini · OpenRouter (`:free`) · NVIDIA NIM — كلهم بواجهة OpenAI عدا Gemini. مفتاح لكل مزوّد `ai_key_<p>` (و`ai_key` = مفتاح الأساسي للتوافق)، الأساسي أولاً ثم بترتيب `PROVIDER_ORDER`. مفتاح مرفوض يتخطّى المزوّد، 429 يبرّد النموذج دقيقة. «اختبر المفاتيح» يكتشف النماذج المتاحة لكل مزوّد الآن ويحفظها `ai_models_<p>` (أسماء النماذج تُسحب — لا تعتمد على اسم ثابت). `_loads` يتحمّل `<think>` ونصاً حول الكائن.

- **ردود قصيرة موجّهة للهدف + قائمة البداية + التحويل الفوري:** النموذج 1-3 أسطر وخطوة تالية واحدة، و`done` يقفل المحادثة بأزرار التقييم. قائمة «محتاج إيه؟» (`flow_engine.MENU`: دعم فني · استفسار · اعرف أكتر · محتاج مساعدة — «قائمة» واتساب التفاعلية لـ 4-10 خيارات) بمفتاح `ai_menu` (افتراضياً لمساعد المنصة)، والاختيار يُمرَّر هدفاً (`GOAL_RULES`) لكل رد بعده. **التحويل الفوري** (`_escalate`: تولٍّ مؤقت 30 دقيقة + تنبيه تليجرام): 3 رسائل متتالية يصنّفها النموذج `on_topic=false` (قبل الحجز — بلا خصم) · أكثر من `ai_max_turns` (10) رسالة في 20 دقيقة · أداة `escalate`.

- **البوتان الرسميان على الصفحة الرئيسية** (قسم `#official` بعد الحقائق، `OfficialBots` في `sections.jsx` + نسخة الخادم في `public.html`): رابط وQR لكل قناة من `app._official_bots` — واتساب من `official_wa_number` أو رقم بوت المساعد الرسمي (`bot_username`)، تليجرام من يوزر بوت المنصة (`platform_tg_username` يُحفظ عند تشغيله). ما لا يُعرف لا يُعرض. **هوية بوت المنصة على تليجرام** (`platform_bot.brand` عند كل تشغيل): وصف ووصف قصير عربي/إنجليزي، أوامر `/start /menu /plans /support`، وأوامر الإدارة في محادثة الأدمن وحده؛ لوحة الأزرار عمودان لـ 4+ خيارات.

- **محرك النمو (2026-09-19)** — الخطة الكاملة: [business/BotYalla_Growth_Plan_AR.pdf](business/BotYalla_Growth_Plan_AR.pdf). قرار المالك: **لا مراسلة لقوائم أرقام مشتراة** (سياسة Meta + القانون 151/2020) — العميل يبدأ المحادثة من إعلان Click-to-WhatsApp/Telegram. `segments.py` (6 شرائح: market · gold · factory · company · center · brand) ← صفحات `/for/<code>` (في الـ sitemap وعدّ الزوار) + رسالة واتساب «مرحبا #code» / تليجرام `?start=seg-code` ← حدث `src_seg_<code>` + ترحيب الشريحة + سياقها للنموذج. **الموافقة على العروض**: بعد تقييم إيجابي («أيوه ابعتلي» ← `bot_users.optin_at`، والإيقاف يسقطها) + تصدير `/bot/<id>/export/optins`. **«روابط الحملات»** `/admin/growth` (للمالك): روابط كل شريحة + QR + المحادثات منها.

### الصفحة الرئيسية (2026-09-13)
- **البطل «ضغطة واحدة»** (`frontend/src/Hero.jsx`): النافذة أولاً ثم الكلام تحتها. نافذة محادثة كبيرة (بلا دوائر/مسرح): تبويبات أربعة أنشطة (`i18n.LANDING2["hero"]`) ← شاشة إنشاء تليجرام تنزلق من أسفل (اسم يُكتب ← يوزر ← «إنشاء») ← البوت يرد بأزراره؛ تدور تلقائياً كل 6ث حتى يختار الزائر أو تخرج من الشاشة. تحتها العنوان + جملة + الأزرار. QR حقيقي للتسجيل على الكمبيوتر (`BY.heroQr` من `segno` في `home()`). تقليل الحركة = البوت مكتمل بلا تسلسل.
- **العرض الحيّ بمبدّل القناة** انتقل من البطل إلى قسم القنوات؛ ضمانات «بدون بطاقة…» انتقلت للنداء الأخير.
- **المحتوى محدَّث للمزايا الجديدة:** Bento (بضغطة+QR · وكيل الإعداد · عقل البوت · صندوق الوارد · وسائط · مصادر العملاء)، الخطوات `lp2_s*` (lp_step* باقية لمعالج التوكن في اللوحة)، قصة من 5 مراحل (تدخّل صاحب النشاط)، سؤالان جديدان (`{ai_price}` يُملأ في الخادم)، الثقة (تشفير التوكنات).
- **سطور الباقات المولَّدة:** `app._plan_feature_lines` تضيف وكيل الإعداد/عقل البوت/صندوق الوارد من `plans.FEATURES` (نسخ، لا تعديل على `PLANS`) — الرقم المعروض = الحدّ المطبَّق.
- ⚠️ الرئيسية تَعِد بالإنشاء بضغطة: فعّل Bot Management Mode قبل النشر.
- **أيقونات «BotYalla Duo»** (`icons.py`، 58 أيقونة مرسومة للمنصة): شبكة 24، خط 1.7، «الأشياء» بطبقة تعبئة ناعمة `_D` (currentColor 22%)، «الأفعال» خط واحد. **لا إيموجي كرموز في الواجهة** — العروض تستخدم `icon`/`i` بأسماء من `icons.py` (اختبار `test_the_landing_draws_icons_not_emoji`). إيموجي رسائل بوت تليجرام نفسها باقية (محتوى محادثة). أيقونة جديدة للرئيسية تُضاف أيضاً إلى `_LANDING_ICONS`.

### الأمان
CSRF شامل (باستثناء `/wh/whatsapp` — التوقيع هو الحارس) · **CSP بـ nonce يتغيّر مع كل طلب** (`script-src` بلا `'unsafe-inline'`) وHSTS على HTTPS فقط — تصدر من التطبيق لا من nginx · تحديد محاولات بدلاء منفصلة على الدخول والتسجيل وفحص أكواد الخصم · `USERNAME_RE` تحصر اسم المستخدم في محارف آمنة · `ProxyFix(x_for=1)` خلف nginx · عزل صلاحيات · الملكية مفروضة **داخل الاستعلام** (`_owned`) لا بفحص لاحق · SQL بمعاملات · `werkzeug.security` للتجزئة.
> **البنود المفتوحة موثّقة في [REVIEW.md](REVIEW.md)** — اقرأه قبل أي عمل على الأمان.

### التوثيق والنشر
`docs/` (USER_GUIDE / ARCHITECTURE / SECURITY / DEPLOYMENT / HOSTINGER / PRODUCTION / **WHATSAPP**) · `deploy/` (`hostinger_deploy.sh`, `deploy_hostinger.bat`, `backup.sh`, systemd, nginx, gunicorn, Dockerfile) · خطة تسويق (PDF) + دليل هوية (PDF).

---

## 3) قرارات مهمة (السبب وراءها)
- **SQLite لا PostgreSQL:** بساطة ونشر أسهل؛ كافٍ لآلاف المستخدمين. `database.py` معزولة للترقية لاحقاً.
- **تليجرام polling وواتساب webhook:** طبيعة كل منصة. ولأن بوتات تليجرام تعمل داخل عملية الويب، يبقى `workers=1` في gunicorn (مع `threads=4`). التوسّع الأفقي = فصل مدير البوتات في خدمة مستقلة.
- **React كطبقة عرض فقط:** لا SPA — النماذج تبقى POST عادية بـ CSRF، فلا تتغيّر أي ضمانة أمنية عند تبديل الواجهة.
- **واتساب مغلق على الباقة المجانية ومحدود بعدها:** Meta تحاسب لكل رسالة، وبلا حدّ قد يتجاوز ما ندفعه عن عميل نشط قيمة اشتراكه.
- **لا نثق في نوع الملف المُعلَن:** `media_store` يشمّ البايتات الأولى — الامتداد لا يصنع نوعاً.
- **لا OCR إلزامي:** يعمل على السيرفر فقط؛ المراجعة اليدوية للصورة هي الضمان الأساسي.
- **الأدمن الافتراضي فقط لو القاعدة فاضية** — لا يتكرر ولا يُنشأ فوق مستخدمين موجودين.
- **مفتاح AI على مستوى المنصة** — العملاء يستخدمون وكيل المالك دون رؤية المفتاح.
- **المبلغ يُحسب في الخادم دائماً** — قاعدة غير قابلة للتفاوض (`AGENTS.md §3.3`).

---

## 4) بيانات ثابتة يجب معرفتها
- **الأدمن الافتراضي:** يُضبط عبر `ADMIN_USER`/`ADMIN_PASS` في `.env` (الافتراضي `admin`/`admin1234` — غيّرها فوراً). **لا تكتب كلمة مرور حقيقية في أي ملف متتبَّع في git.**
- **بيانات الدفع (مضبوطة مسبقاً في `platform`):** فودافون كاش `01097585951` · انستاباي `youssefalsherief@instapay` (+ QR في `static/instapay_qr.jpg`) · بنك Mashreq.
- **تواصل الدعم:** `support_email=info@botyalla.com` · `support_whatsapp=201097585951` · `support_telegram` (فارغ — يملؤه الأدمن).
- **بوت تنبيهات الدفع:** يحتاج توكن بوت + `admin_chat_id` من إعدادات المنصة (بدونه لا تصل تنبيهات تليجرام ولا تعمل التذكيرات، لكن كل شيء يظهر في لوحة الأدمن).
- **واتساب:** يحتاج `wa_verify_token` و`wa_app_secret` في إعدادات المنصة — **بدونهما الويبهوك يرفض كل شيء عمداً (fail-closed)**.
- **الإيميل:** `SMTP_HOST` · `SMTP_PORT` · `SMTP_USER` · `SMTP_PASS` · `SMTP_FROM` · `SMTP_TLS` · `SMTP_REPLY_TO` (اختياري — وإلا إيميل الدعم) في `.env`، والترتيب المعتمد: **Brevo يرسل** (`no-reply@` بلا صندوق — يكفي توثيق الدومين في Brevo) و**Hostinger يستقبل** على `info@botyalla.com` (Reply-To). حتى 2026-09-14 كان توثيق Brevo ناقصاً (لا DKIM، والـ SPF بلا Brevo) — الخطوات في docs/EMAIL_DNS.md، و**`PUBLIC_URL`** (مثل `https://botyalla.com`) — بدونه لا تُرسل روابط الاسترجاع عمداً.

---

## 5) ما تبقّى / أفكار تطوير (بالأولوية)
> **قائمة الإطلاق الكاملة وحالتها في [business/LAUNCH_READINESS.md](business/LAUNCH_READINESS.md)** — هي المرجع؛ ما يلي ملخّص.

1. **إجراءات تشغيلية** (خارج الكود): تدوير `ADMIN_PASS` (ما زال المسرّب) · `COOKIE_SECURE=1` · ضبط SMTP و`PUBLIC_URL`.
2. **الباقات الجديدة والدفع السنوي** (L-07/L-08) — بعد مراجعة تسعير واتساب (L-13، قبل 1 أكتوبر 2026).
3. **onboarding** (L-10) · اختبار حمل لسقف البوتات (L-11) · استعادة نسخة احتياطية فعلياً (L-15).
4. **دفع أونلاين تلقائي** (Paymob/Fawry) لتحصيل الاشتراكات بدون مراجعة يدوية.
5. **قوالب بوت إضافية:** عقارات، مطعم بمنيو مصوّر، حجز فعاليات.
6. **تصدير Excel** بجانب CSV، وتقارير أعمق.
7. **باني فلو بالسحب والإفلات** + تفريعات شرطية.
8. **فصل مدير البوتات** في خدمة مستقلة ⇒ تعدّد workers عند النمو الكبير.

> عند تنفيذ أي بند: اتبع `AGENTS.md` حرفياً، أضِف الترجمة والأيقونات، **واختبر** قبل التسليم.

---

## 6) سير العمل السريع
```bash
./run_tests.sh          # كل الاختبارات — عملية مستقلة لكل ملف
python tests/test_x.py  # ملف واحد
cd frontend && npm run build   # بعد أي تعديل على الواجهة (ينتج static/dist/)
```
- تعديل → **اختبار** → مزامنة الملفات المتغيّرة → `git push` → (نشر عبر `deploy_hostinger.bat` عند الحاجة).
- **لا تشغّل `pytest tests/` على المجلد كله:** `database.DB_PATH` يُقرأ عند الاستيراد، فتتشارك الملفات أول قاعدة وتتصادم. `run_tests.sh` يعطي كل ملف عمليته.
- لا تعدّل ملفات الوثائق (`docs/`, `LICENSE`, `README`) إلا لو التغيير يخصّها فعلاً.
