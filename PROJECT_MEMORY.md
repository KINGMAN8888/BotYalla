# PROJECT_MEMORY.md — ذاكرة مشروع BotYalla

> ملف السياق الكامل للمشروع: ما هو، ما تم إنجازه، القرارات المهمة، وما تبقّى. اقرأه أنت والوكيل الذكي قبل أي عمل، مع `AGENTS.md`.

---

## 1) ما هو المشروع
**BotYalla** — منصة SaaS عربية/إنجليزية تتيح لأصحاب الأنشطة إنشاء وتشغيل **بوتات تليجرام بدون برمجة**، مع اشتراكات ودفع محلي. المالك: **Youssef Alsherief** (info@youssefalsherief.tech · 201097585951 · youssefalsherief.tech).

- التقنية: Python + Flask + SQLite + python-telegram-bot. عربي/إنجليزي (RTL/LTR). تصميم داكن عصري.
- النموذج الربحي: باقات شهرية (مجانية/احترافية 199ج/الأعمال 499ج) + دفع محلي (فودافون كاش/انستاباي/بنكي) + بوتات مخصّصة عند الطلب.

---

## 2) الحالة: ✅ منجز ومختبَر
- **الحسابات والأدوار (RBAC):** admin / support / user. أول مستخدم = admin. حساب افتراضي `admin/admin1234` عند أول تشغيل (قابل للتغيير عبر `.env`). صفحة «حسابي» لتعديل الاسم/كلمة المرور. الأدمن يضيف حسابات بأدوار من لوحة المستخدمين.
- **قوالب البوتات (7):**
  - `flow` باني محادثات No-Code · `store` متجر · `booking` حجوزات · `customer_service` خدمة عملاء.
  - `faq` أسئلة شائعة (محرّك قائمة menu) · `feedback` تقييم وآراء · `support` دعم فني/تذاكر — **الثلاثة الأخيرة بقوالب افتراضية جاهزة** (`PRESET_FLOWS` / `DEFAULT_MENU_ITEMS`).
- **باني الفلو (No-Code):** لأنواع `flow/customer_service/feedback/support`. ومحرّر قائمة (سؤال←إجابة) لـ `faq`.
- **وكيل الـ AI:** يضبط البوت من وصف النشاط (Gemini افتراضي / Groq) + مولّد احتياطي بلا مفتاح. **المفتاح للأدمن فقط** (إعداد منصة).
- **التكامل الرسمي مع تليجرام:** عند إنشاء/حفظ بوت، تُضبط رسمياً عبر Bot API: `setMyName`/`setMyShortDescription`/`setMyDescription`/`setMyCommands` (`tg_helpers.configure_bot_profile`). زر «مزامنة مع تليجرام». **لا يوجد إنشاء بوت عبر API — BotFather يدوي.**
- **التحقق من التوكن + ربط الأدمن بضغطة** (deep-link `?start=owner-CODE`) + أمر `/id`.
- **الدفع والتحقق المزدوج:** رفع اسكرين → فحص آلي (صورة صحيحة + بصمة تكرار SHA-256 + OCR اختياري) → **تنبيه تليجرام بزرّي موافقة/رفض** + صفحة أدمن للمدفوعات (تعرض الصورة + الفحص المفصّل). **لا تفعيل تلقائي.**
- **الاشتراكات:** 30 يوماً (شهري) من تاريخ الاشتراك، تنتهي تلقائياً. العرض بتاريخ حقيقي + أيام متبقية + «يتجدّد شهرياً». الأدمن «غير محدود».
- **التحليلات + البث (Broadcast) + تصدير CSV + دعم صور (ترحيب/منتجات).**
- **لوحة أدمن كاملة:** نظرة عامة (KPIs + رسوم)، مستخدمون (أدوار/حظر/باقة)، مدفوعات (موافقة/رفض)، طلبات مخصّصة.
- **بوت مخصّص:** كارت في الداش بورد → صفحة طلب (نموذج يتخزّن + إشعار تليجرام) + أزرار تواصل مباشر (إيميل/واتساب/تليجرام). لوحة أدمن للطلبات مع حالات (جديد/قيد التنفيذ/منجز/مرفوض).
- **بوت المنصة (للأدمن):** أوامر `/stats` `/pending` (بأزرار) `/users` `/revenue` + إشعار بكل حركة (تسجيل/بوت جديد/دفعة/طلب مخصّص).
- **الأمان:** CSRF شامل، تحديد محاولات دخول، تأمين كوكيز، عزل صلاحيات، خصوصية الإيصالات، SQL بمعاملات.
- **الديكومنتيشن:** `docs/` (USER_GUIDE / ARCHITECTURE / SECURITY / DEPLOYMENT / HOSTINGER). ملفات نشر (`deploy/`: `hostinger_deploy.sh`, `deploy_hostinger.bat`, `backup.sh`, systemd, nginx, gunicorn, Dockerfile) + `docs/PRODUCTION.md`. خطة تسويق وربح (PDF) + دليل هوية (PDF).

---

## 3) قرارات مهمة (السبب وراءها)
- **SQLite لا PostgreSQL:** بساطة ونشر أسهل؛ كافٍ لآلاف المستخدمين. طبقة `database.py` معزولة للترقية لاحقاً.
- **Polling لا Webhooks:** أبسط للإطلاق؛ لذلك `workers=1` في gunicorn (حالة البوتات في الذاكرة). التوسّع الأفقي = فصل مدير البوتات في خدمة مستقلة (مستقبلاً).
- **لا OCR إلزامي:** يعمل على السيرفر (tesseract يُثبَّت في `deploy/hostinger_deploy.sh`) وليس محلياً على ويندوز؛ المراجعة اليدوية للصورة هي الضمان الأساسي.
- **الأدمن الافتراضي فقط لو القاعدة فاضية** — لا يتكرر ولا يُنشأ فوق مستخدمين موجودين.
- **مفتاح AI على مستوى المنصة** — العملاء يستخدمون وكيل المالك دون رؤية/إدارة المفتاح.

---

## 4) بيانات ثابتة يجب معرفتها
- **الأدمن الافتراضي:** `admin` / `admin1234` (غيّرها فوراً). قابل للضبط عبر `ADMIN_USER`/`ADMIN_PASS` في `.env`.
- **بيانات الدفع (مضبوطة مسبقاً في `platform`):** فودافون كاش `01097585951` · انستاباي `youssefalsherief@instapay` (+ QR في `static/instapay_qr.jpg`) · بنك Mashreq (Youssef Mahmoud Saber Mahmoud / 059101546889 / IBAN EG160046020400000059101546889).
- **تواصل الدعم:** `support_email=info@youssefalsherief.tech`, `support_whatsapp=201097585951`, `support_telegram` (فارغ — يملؤه الأدمن لتفعيل زر تليجرام).
- **بوت تنبيهات الدفع:** يحتاج توكن بوت + `admin_chat_id` من إعدادات المنصة ليعمل (بدونه لا تصل تنبيهات تليجرام، لكن كل شيء يظهر في لوحة الأدمن).

---

## 5) ما تبقّى / أفكار تطوير (بالأولوية)
1. **رفع صور مباشر** بدل روابط URL (ترحيب/منتجات) — تخزين في `uploads/` وتقديمه بأمان.
2. **دعم واتساب** (WhatsApp Business API) — أكبر فرصة سوقية بعد إثبات النموذج على تليجرام.
3. **دفع أونلاين تلقائي** (Paymob/Fawry) لتحصيل الاشتراكات بدون مراجعة يدوية.
4. **تجديد/تنبيهات الاشتراك** (إيميل/تليجرام قبل الانتهاء) + صفحة تجديد.
5. **قوالب بوت إضافية:** عقارات، مطعم بمنيو مصوّر، حجز فعاليات.
6. **تصدير Excel** بدل/بجانب CSV، وتقارير أعمق.
7. **باني فلو بالسحب والإفلات** + تفريعات شرطية.
8. **Webhooks + تعدد workers** عند النمو الكبير.

> عند تنفيذ أي بند: اتبع `AGENTS.md` حرفياً، أضِف الترجمة والأيقونات، **واختبر** قبل التسليم.

---

## 6) سير العمل السريع
- تعديل → اختبار (Flask test client، راجع `AGENTS.md §5`) → مزامنة الملفات المتغيّرة → `git push` → (نشر عبر `deploy_hostinger.bat` عند الحاجة).
- لا تعدّل ملفات الوثائق (`docs/`, `LICENSE`, `README`) إلا لو التغيير يخصّها فعلاً.
