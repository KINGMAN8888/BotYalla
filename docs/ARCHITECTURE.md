# 🏗️ البنية التقنية — BotYalla

## نظرة عامة
منصة SaaS لإنشاء وتشغيل بوتات **تليجرام وواتساب** بدون كود، مع صندوق وارد مشترك، اشتراكات ودفع محلي، ومحفظة للرسائل التسويقية.
**Python + Flask + SQLite + python-telegram-bot** — بلا اعتماديات ثقيلة.

## المكوّنات (الملفات)
| الملف | المسؤولية |
|---|---|
| `app.py` | خادم Flask — كل المسارات، الجلسات، CSRF، فرض الباقات |
| `database.py` | طبقة SQLite (users, bots, subscriptions, payments, events, platform...) |
| `auth.py` | تشفير كلمات المرور (Werkzeug) |
| `plans.py` | تعريف الباقات وحدودها |
| `payments.py` | محرك الإيصالات: فحص الصورة، بصمة التكرار، وOCR يرفض ما ليس إيصالاً لحسابنا بهذا المبلغ **قبل** الحفظ (قراءتان متزامنتان على الأكثر) |
| `platform_bot.py` | بوت المنصة — تنبيه الأدمن بأزرار موافقة/رفض، وردود تذاكر الدعم بـ Reply |
| `bot_manager.py` | تشغيل عدة بوتات + بوت المنصة في حلقة asyncio بخيط منفصل · الحملات في خيوط خلفية |
| `flow_engine.py` | محرك المحادثات العام (No-Code) + «عقل البوت» (ردود AI بحصة/محفظة) |
| `templates_bot.py` | قوالب البوتات: flow / store / booking / customer_service / faq / feedback / support |
| `channels/` | طبقة القنوات: `telegram.py` · `whatsapp.py` (Cloud API عبر httpx، ويبهوك `/wh/whatsapp` بتوقيع `wa_app_secret`) · `wa_templates.py` (قوالب Meta). المحرك يرسل عبر واجهة واحدة |
| `managed_bots.py` | الإنشاء بضغطة (Telegram Managed Bots) عبر بوت المنصة |
| `support_desk.py` | تذاكر الدعم والشكاوى وطلبات «ربط واتساب» — تنبيه فوري للأدمن |
| `media_store.py` / `asset_store.py` | ملفات العملاء الواردة · مكتبة وسائط صاحب البوت |
| `tg_helpers.py` | التحقق من التوكن + ربط الأدمن + أوامر مشتركة |
| `ai_agent.py` | وكيل الإعداد (Gemini/Groq) + مصمّم احتياطي بلا اتصال |
| `i18n.py` / `icons.py` | الترجمة (عربي/إنجليزي) + مجموعة الأيقونات |

## نموذج البيانات (أهم الجداول)
- **users** — حسابات لوحة التحكم (كلمة مرور مشفّرة).
- **bots** — كل بوت: توكن مشفّر (Fernet)، القناة (telegram/whatsapp)، قالب، إعدادات JSON، المالك.
- **subscriptions** — باقة المستخدم + تاريخ الانتهاء.
- **payments** — كل دفعة: طريقة، مبلغ، صورة، بصمة، نتيجة الفحص الآلي، الحالة (pending/approved/rejected).
- **wallet / wallet_ledger** — محفظة الرسائل التسويقية بالقروش الصحيحة، وكل حركة مقيّدة.
- **messages / conversations** — صندوق الوارد: كل رسالة داخلة وخارجة، ووضع المحادثة (بوت/تولٍّ بشري).
- **tickets / ticket_msgs** — الدعم والشكاوى وطلبات ربط واتساب.
- **platform** — إعدادات المنصة (بيانات الدفع، توكن بوت التنبيهات، معرّف المالك).
- **events** — أحداث للتحليلات (start/lead/order/booking) — يُحتفظ بها سنة.
- **bot_users** — مشتركو كل بوت (للبث الجماعي).

## تشغيل البوتات
`BotManager` يشغّل حلقة `asyncio` في **خيط منفصل** عن Flask. كل بوت تليجرام `Application` مستقل يعمل بـ polling،
وبوتات واتساب بلا polling: الويبهوك يسلّم رسائلها للحلقة نفسها.
بوت المنصة يعمل بنفس الحلقة مع `CallbackQueryHandler` لأزرار الموافقة/الرفض.

**الحملات (البث):** تُرسل في خيط خلفي — حملة واحدة لكل بوت — والطلب يرجع فوراً. كل حلقة إرسال تحدّث عدّاداً
حيّاً بعد كل رسالة، ومنه تُحسب النتيجة: ما وصل يُحاسَب وما لم يصل يُردّ للمحفظة، حتى عند انتهاء المهلة.

> **قيد التوسّع:** الحالة في الذاكرة ⇒ عملية Gunicorn واحدة (`workers=1`). للتوسّع الأفقي:
> افصل مدير البوتات في خدمة مستقلة تتواصل مع الويب عبر قاعدة البيانات/طابور رسائل.

## تدفّق الدفع (التحقق المزدوج)
```
المستخدم يختار باقة → صفحة الدفع (QR + بيانات) → يدفع خارجياً → يرفع اسكرين
        │
        ▼
[طبقة 1: فحص آلي]  صورة صحيحة؟ + بصمة SHA-256 (تكرار؟) + OCR اختياري (المبلغ)
        │  (استشاري فقط — لا يفعّل شيئاً)
        ▼
[طبقة 2: بوت المنصة]  تنبيه للأدمن بالصورة + زرّي ✅/❌
        │
        ▼
موافقة الأدمن (هو فقط) → تفعيل الاشتراك 30 يوماً (ذرّي، غير قابل للتكرار)
```

## قابلية النقل لقاعدة بيانات أكبر
SQLite كافٍ لآلاف المستخدمين. للانتقال لـ PostgreSQL لاحقاً: طبقة `database.py` معزولة، فالتغيير محصور فيها.
