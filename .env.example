# انسخ هذا الملف إلى .env وعدّل القيم — لا ترفع .env إلى GitHub
FLASK_SECRET=ضع_هنا_سراً_عشوائياً_طويلاً
COOKIE_SECURE=1
# لتوليد سر قوي:  python -c "import secrets;print(secrets.token_hex(32))"

# حساب الأدمن الافتراضي (يُنشأ أول تشغيل فقط)
ADMIN_USER=admin
ADMIN_PASS=غيّر_كلمة_المرور_دي
